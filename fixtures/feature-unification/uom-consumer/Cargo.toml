[package]
name = "qubit-measure-feature-unification-uom"
version = "0.0.0"
edition = "2024"
rust-version = "1.94"
publish = false

[dependencies]
qubit-measure = { path = "../../..", features = ["uom"] }
rust_decimal = "1.39"
serde = "1.0"
uom = { version = "0.38", default-features = false, features = ["f64", "si", "std"] }

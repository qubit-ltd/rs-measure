[package]
name = "qubit-measure"
version = "0.3.0"
edition = "2024"
rust-version = "1.94"
authors = ["Haixing Hu <starfish.hu@gmail.com>"]
license = "Apache-2.0"
repository = "https://github.com/qubit-ltd/rs-measure"
description = "Persistent typed Decimal measurements with explicit units and optional uom adapters"
homepage = "https://github.com/qubit-ltd/rs-measure"
documentation = "https://docs.rs/qubit-measure"
readme = "README.md"
keywords = ["measurement", "units", "uom", "quantity", "qubit"]
categories = ["data-structures", "science"]
include = [
    "/Cargo.toml",
    "/LICENSE",
    "/README.md",
    "/README.zh_CN.md",
    "/examples/**",
    "/src/**",
    "/tests/**",
]

[features]
default = []
uom = ["dep:uom"]

[lib]
name = "qubit_measure"

[[example]]
name = "uom_bridge"
required-features = ["uom"]

[dependencies]
rust_decimal = { version = "1.39", features = ["macros", "serde-with-str"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
uom = { version = "0.38", optional = true, default-features = false, features = ["f64", "si", "std"] }

[dev-dependencies]
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
